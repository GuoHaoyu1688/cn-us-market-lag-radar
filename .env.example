# Optional A-share history fallback. Leave empty for the default source order.
A_STOCK_KLINE_SOURCE=

# Reuse existing local CN candles during development: 1/true/yes.
MARKET_LAG_REUSE_CN_CANDLES=

# X API is disabled by default because it may incur charges.
X_ENABLE_PAID_API=0
X_BEARER_TOKEN=

# Optional AI explanation command in scripts/a_stock_cli.py.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4.1-mini
