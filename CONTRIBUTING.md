# Contributing

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 不要提交真实行情快照、账户数据、密钥、日志或本机路径。
3. 运行测试和脱敏检查。
4. 在 Pull Request 中说明点时数据边界、验证区间和失败条件。

```bash
python -m pip install -r requirements.txt
PYTHONPATH=scripts python -m unittest discover -s tests -p 'test_*.py'
python scripts/sanitize_check.py
```

## 模型贡献要求

- 使用时间顺序切分，禁止随机打乱时间序列后报告结果。
- 特征必须在预测时点可得；财务、新闻和行业成分需要说明发布日期或生效日。
- 固定主终点后再比较模型，不可事后选择收益最好的持有期。
- 同时报告 Brier、校准误差、收益误差、区间覆盖、样本数和交易成本后结果。
- 明确展示基准、拒绝预测比例和最差分位，不接受只报告命中率。
- 新模型必须有前向账本兼容方案，并附至少一个失败测试。

## 前端贡献要求

- A 股和美股主题需保持清晰区分。
- 不得把诊断分显示成上涨概率或确定性承诺。
- 桌面与移动宽度均不得出现文本穿模或横向页面溢出。
- 演示模式必须始终显示“合成演示数据”提示。
